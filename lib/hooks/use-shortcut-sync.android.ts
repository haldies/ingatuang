/**
 * Android Placeholder for Shortcut Sync
 * Android currently uses Widgets and doesn't need a background-to-foreground 
 * JSON sync bridge like Siri Shortcuts on iOS.
 */
export function useShortcutSync() {
  // Do nothing (Android logic is handled elsewhere in Widgets)
  return null;
}
